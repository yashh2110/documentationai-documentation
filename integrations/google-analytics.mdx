---
title: Google Analytics
description: >-
  Track page views and events on your documentation site with Google Analytics 4.
---
Google Analytics 4 (GA4) tracks page views, user engagement, and custom events across your documentation site. Enable the integration to start collecting data with your existing GA4 property.

## Prerequisites

- A [Google Analytics](https://analytics.google.com/) account with a GA4 property

- Your GA4 measurement ID (starts with `G-`)

- Publishing access to your documentation project

## Configure from the Editor

<Steps>
  <Step title="Open Site Config" icon="settings" title-type="p">
    In the Editor, click **Site Config** in the top toolbar.
  </Step>

  <Step title="Navigate to Integrations" icon="plug" title-type="p">
    Select **Integrations** from the left sidebar.
  </Step>

  <Step title="Enable Google Analytics" icon="toggle-right" title-type="p">
    Select **Google Analytics** and enter your GA4 measurement ID.
  </Step>

  <Step title="Publish" icon="upload" title-type="p">
    Click **Publish** to deploy the integration to your live site.
  </Step>
</Steps>

## Configure via documentation.json

Add the `ga` key under `integrations` in your `documentation.json`:

```json
{
  "integrations": {
    "ga": {
      "measurementId": "G-XXXXXXXXXX"
    }
  }
}
```

| Property | Type | Required | Description |
| -------- | ---- | -------- | ----------- |
| `measurementId` | string | Yes | Your GA4 measurement ID (for example, `G-XXXXXXXXXX`) |

## Where to find your measurement ID

1. Sign in to [Google Analytics](https://analytics.google.com/).

2. Go to **Admin** → **Data Streams** → select your web stream.

3. Copy the measurement ID displayed at the top (format: `G-XXXXXXXXXX`).

## What's next

- [Analytics and Chat Integrations](/integrations/overview) — overview of all supported integrations

- [Google Tag Manager](/integrations/google-tag-manager) — manage multiple tags from a single container

- [Analytics overview](/analytics/overview) — view traffic and engagement data for your documentation
